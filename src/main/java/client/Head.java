package client;

public class Head {
	
	public static void main(String[] args){
		App.launch(App.class, args);
	}

	/*
	 * TODO
	 * Add new icons for just the plain installer
	 * Create way of verifying the whole file was sent correctly between the client and the server
	 * 
	 */
}
